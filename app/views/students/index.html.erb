<h1>Students</h1>

<% @students.each do |student| %>
    <ul>
        <li><%= link_to student.to_s, student_path(student) %>
           <%= link_to 'Edit', edit_student_path(student) %></li>
    </ul>
<%end%>
<br>
<%= link_to "Add Student", new_student_path %> 